# Task
1. Devices testing must also use gui, and settings menu should have device choosing submenus and test device menu item where 2 seconds of sine wave is played, than 5 seconds of microphone stream. No cli onboarding device test.
2. Read UV documentation if it supports environment variables setting for windows: before building ggwave we need `GGWAVE_USE_CYTHON=1`.
3. the file menu should have send file item, or ctrl+o.
4. Protocol setting should have protocol names instead of numbers. Instead of protocol submenu, make it a dialog with protocol list and payload length box that doesn't allow zero for some protocols (see cli).
5. Make a feature to send protocol change command to another device, obviously using the old protocol. Like hey device, i changed to protocol 9 with payload length 64! Or without payload length if the protocol doesn't require it. This is for testing and debugging, but also for users who want to change protocol on the fly without going to settings menu on another device. The program must be able to send and receive this types of commands. In settings menu you can choose to send this command or not.
6. Make a new feature: remote control commands. It must be able to shutdown the pc, restart, exit the app, open website, play music etc. You can assign remote commands to hotkeys and they must be customizable and available in remote menu. The other device must enable receiving remote commands in settings menu to avoid malicious use. You can make remote commands in a py addon like file which has execute function that receives command name and arguments, and you can add commands there. This is for testing and fun, but also for users who want to control another device with ggwave. Pyinstalled app must also be able to see plugins folder and import them.
7. Devices.json must now be settings.json with other mentioned settings. It must be in appdata.
8. Implement accessible_output3 for screenreaders. (it's for)k of accessible_output2). It must speak received text, file updates, protocol changes etc what's needed for screenreaders.