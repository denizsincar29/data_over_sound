# Task
1. When choosing protocol, the payload length must be automatically selected on the protocols 9-11 with no option to specify -1. Actually, -1 for default is a bad practice, use checkbox "specify payload length" and if checked, then show the input field for payload length, otherwise hide it and use default value of -1. Make it able to select from 4 to 64. On protocols 9-11, it's autochecked and checkbox is hidden, showing payload length 32 by default. On other protocols, the checkbox is shown and unchecked by default, with payload length input field hidden until checkbox is checked. So no error messages needed when it's invalid, just don't allow it to be invalid.
2. Fix accessible output 3 bugs if it's used incorrectly in the code. Read docs and fix it. It's not speaking.
3. In remote commands, make something like a create new command menu item that creates the plugin's folder with __init__.py with a sample function that speaks hello world.
4. The plugin should be able to use the api of the main app by using some kind of object that is passed to the plugin. I think even the plugin should be a class inherited by BasePlugin... It's up to you how you implement it.